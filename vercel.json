{
  "framework": "vite",
  "installCommand": "npm install --prefix apps/ai-workspace",
  "buildCommand": "npm run build --prefix apps/ai-workspace",
  "outputDirectory": "apps/ai-workspace/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
