# Extensibility

Replace InMemory `*ProviderInterface` implementations with Tenant DB adapters without changing managers.

Next sprint can wire Planner/Gateway on top of `ToolAccessGuard` + `TenantAiContext` without architectural changes.
