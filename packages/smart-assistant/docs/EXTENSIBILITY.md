# Extensibility

Extension points (`ExtensionPoints`): agent · channel · ai_model · integration · capability · automation · knowledge_provider

Add by **registering** against registry contracts — never by editing frozen core classes.
